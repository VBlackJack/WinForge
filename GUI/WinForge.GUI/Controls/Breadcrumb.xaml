<UserControl x:Class="WinForge.GUI.Controls.Breadcrumb"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <ItemsControl x:Name="BreadcrumbItems">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Separator (not for first item) -->
                    <ui:SymbolIcon Symbol="ChevronRight24"
                                     Width="20" Height="20"
                                     Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                     VerticalAlignment="Center"
                                     Visibility="{Binding IsFirst, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                    <!-- Breadcrumb item -->
                    <ui:Button Style="{DynamicResource CompactTransparentButton}"
                            Content="{Binding Label}"
                            Command="{Binding NavigateCommand}"
                            IsEnabled="{Binding IsClickable}"
                            Padding="8,4"
                            FontWeight="{Binding FontWeight}"
                            AutomationProperties.Name="{Binding Label}"/>
                </StackPanel>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
